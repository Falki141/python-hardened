# python-hardened

Docker image with Python (latest official tag) running from a `scratch` final image.

## Build

```bash
docker build -t python-hardened .
```

## Run

```bash
docker run --rm python-hardened
```